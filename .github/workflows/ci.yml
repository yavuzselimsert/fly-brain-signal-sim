name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Check syntax
        run: |
          python -m py_compile lif_neuron.py
          python -m py_compile build_graph.py
          python -m py_compile analyze_graph.py
          python -m py_compile network_simulation.py

      - name: Run tests
        run: |
          python -m pytest test_fly_brain.py -v